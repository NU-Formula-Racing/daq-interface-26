import React from 'react';
import ReactDOM from 'react-dom/client';
import TableWindow from './components/Table.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TableWindow />
  </React.StrictMode>
);